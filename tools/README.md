# Tools

This folder is for generators, linters, validation scripts, and local developer tooling that support the monorepo.

## Intended structure
- `scripts/`
- `linters/`
- `generators/`

## Guardrail
Keep product-specific rules in source docs or contracts. Tooling should consume those rules, not silently replace them.
