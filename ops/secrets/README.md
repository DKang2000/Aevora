# Secrets Management

This directory contains templates and process guidance only.

## Rules
- No real credentials in the repo.
- Secrets are injected through local untracked env files, GitHub Actions environments, and hosted runtime secret stores.
- Local development should use placeholder values or sandbox-only credentials.
