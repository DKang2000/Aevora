# Scripts

This folder is for root-level helper scripts that do not belong to a specific product surface.

## Typical uses
- repo bootstrap helpers
- validation wrappers
- local convenience scripts

If a script becomes part of the durable tooling surface, move it into `tools/`.
