# ADRs

Use this folder for accepted architectural decision records.

## Rule
- write an ADR when implementation needs to deviate from or clarify the locked architecture
- link the relevant `ST-*`, `IOS-*`, `BE-*`, `OPS-*`, or `QA-*` section ID
- do not use an ADR to silently expand v1 scope
