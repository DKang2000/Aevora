# iOS Configuration Placeholders

This folder is reserved for generated or locally provided Xcode configuration files that should not hold canonical product truth.

## Intended files
- debug and release `.xcconfig` files generated or maintained during iOS foundation setup
- environment-specific endpoint placeholders
- signing-related values supplied outside version control

## Guardrail
Keep secrets and signing material out of the repo. Store only placeholders, templates, or documentation here.
